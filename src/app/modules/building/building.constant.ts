// Searchable fields to GET buildings
const searchableFields = ['title'];

// Searching and Filtering Fields
const filterableFields = ['searchTerm'];

export const BuildingConstants = {
  searchableFields,
  filterableFields,
};
