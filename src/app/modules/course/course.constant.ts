// Searchable fields to GET courses
const searchableFields = ['title', 'code'];

// Searching and Filtering Fields
const filterableFields = ['searchTerm', 'id', 'code'];

export const CourseConstants = {
  searchableFields,
  filterableFields,
};
